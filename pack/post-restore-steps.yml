steps:
- bash: |
    cd $CACHE_PACK_TOOLS_DIR
    node tools.js post-restore
  condition: and(variables.CACHE_PACK_TOOLS_DIR, eq(variables.CACHE_RESTORED, 'true'))
  displayName: 'Cache pack: post-restore'
